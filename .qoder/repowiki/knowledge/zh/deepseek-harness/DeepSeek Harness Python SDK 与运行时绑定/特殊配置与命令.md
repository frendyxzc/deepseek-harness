本地开发需先运行仓库根目录的 `scripts/build-exe-for-python-sdk.ts` 生成 `sdk-runtime/src/deepseek_harness_runtime/runtime/` 下的二进制产物；SDK 通过 `uv.lock` 与 `[tool.uv.sources]` 将 `deepseek-harness-runtime-bin` 以 editable 方式指向同级 `sdk-runtime`，因此必须在该目录下使用 uv/pip 安装才能工作。测试通过 `pytest tests` 运行，位于 `python/sdk/tests/`。