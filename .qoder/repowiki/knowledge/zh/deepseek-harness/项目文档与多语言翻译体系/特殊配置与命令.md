新增或修改文档需同时更新对应 `.zh.md` 与 `.i18n.yaml`，并使用 `pnpm run verify-translation-pairing --write <pair>` 重新记录双方 blob hash；CI 通过 `doc-sync` 运行完整门禁。可用 `pnpm run verify-translation-pairing --list` 查看当前配对状态，`pnpm run resolve-translation-pairing-conflicts` 处理合并冲突。