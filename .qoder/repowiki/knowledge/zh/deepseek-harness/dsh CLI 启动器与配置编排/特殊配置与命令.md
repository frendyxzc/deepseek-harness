通过 `pnpm build`（或等价 tsdown 命令）生成 `lib/bin.js` 作为发布产物；运行 `dsh --profile <name>` 启动 profile，`dsh plugin --profile <name> <pnpm args>` 管理 profile 插件，`dsh --profile <name> --dump-config|--dump-default-config` 输出配置组合。