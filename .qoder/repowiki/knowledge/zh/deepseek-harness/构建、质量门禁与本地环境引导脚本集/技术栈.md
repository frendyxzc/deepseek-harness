Node.js + TypeScript（ESM，`import.meta.dirname`/`import.meta.main` 直接作为 CLI 入口）；Vitest 用于所有 `.spec.ts` 测试；pnpm 工作区通过 `npm_execpath` 反查真实可执行以无 shell 启动子进程；Bash 脚本用于 `setup-dsh` 及少量 CI 辅助；Markdown 处理使用 `mdast-util-*` / `micromark-extension-gfm`。