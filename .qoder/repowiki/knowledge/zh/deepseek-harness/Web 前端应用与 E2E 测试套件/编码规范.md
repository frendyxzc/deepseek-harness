- 每个 E2E 场景以独立的 `<name>.e2e.ts` 文件组织，并通过同目录或 `tests/expected/<scenario>/` 下的 `.expected.md/.txt` 文件声明 UI/几何/文本 golden。
- 测试通过 `launchWebScaffold` 统一启动真实 web 组合，所有环境（DSH_HOME、agents-home、session persistence、webserver 端口、llm 适配器）均被 patch 为临时路径，确保回放可重现。
- 构建期通过 Rollup `manualChunks` 按 npm 包名显式划分 vendor/langs/fonts chunk，React 家族保持 dedupe 以避免多实例导致 hook 身份分裂。
- 通过 Cordis overlay patch.yml（base/web-app 及可选 extraOverlayPath）而非硬编码配置来覆盖 shipped 组合，使测试环境与生产 profile 保持一致的层叠顺序。
- 回放/录制模式下使用 `installLlmReplay` 注入 dsh-llm-replay 适配器，并在 teardown 中调用 `assertConsumed()` 校验 fixture 被完整消费。