Cordis 插件框架（`Service` + `ctx.effect`/`ctx.plugin` + `slots.inject`），React 18 + CSS Modules 组件，tsdown 打包；通过 `dsh.client.inject` 声明对 `@deepseek-ai/dsh-api-*`、`@deepseek-ai/dsh-client-*` 等 workspace peer 依赖的装配需求。