Node 端基于 `@deepseek-ai/cordis` Service 与 `@deepseek-ai/cordis-plugin-loader` 内部 API；浏览器端使用原生 `<script>` 动态加载；状态存储基于 `zustand/vanilla` + `immer`，通过 `subscribeWithSelector` 与自定义 `requestAnimationFrame` 批处理实现观察者通知。