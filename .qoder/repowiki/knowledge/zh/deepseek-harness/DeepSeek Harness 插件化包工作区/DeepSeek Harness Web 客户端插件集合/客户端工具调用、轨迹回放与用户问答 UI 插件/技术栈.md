React 18 + TypeScript；构建使用 tsdown（`tsdown.config.ts`）产出 ESM；UI 状态基于 `@tanstack/react-virtual`（轨迹虚拟滚动）与 `diff`（差异展示）；通过 Cordis `slots`/`sessions`/`uiSession`/`uiConversation`/`locale`/`remote` 服务进行插件间解耦。