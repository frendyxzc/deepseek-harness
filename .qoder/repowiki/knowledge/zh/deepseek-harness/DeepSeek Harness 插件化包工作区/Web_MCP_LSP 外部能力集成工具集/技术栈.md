TypeScript + Cordis 插件框架（Service/Context/effect 生命周期）；配置校验统一使用 `@deepseek-ai/schemastery`；web 搜索通过 Exa/Perplexity/DeepSeek 三方 API，fetch 走匿名 HTTP(S)；MCP 支持 stdio 子进程与 Streamable HTTP (SSE) 两种传输；LSP 通过 stdio 协议驱动本地语言服务器。