基于 `@deepseek-ai/cordis` 服务容器与作用域机制；使用 `node:async_hooks` 的 `AsyncLocalStorage` 维护发起者上下文；Schema 校验统一采用 `@deepseek-ai/schemastery`；JSON 快照与深冻结通过 `@deepseek-ai/dsh-llm` 的 `deepFreeze`/`snapshotJsonValue` 完成。