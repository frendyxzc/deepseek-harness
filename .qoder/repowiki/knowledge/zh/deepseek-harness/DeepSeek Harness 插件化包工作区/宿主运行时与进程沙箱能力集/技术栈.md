TypeScript + Cordis 服务框架；Linux 沙箱使用 bubblewrap 与 Landlock syscall，macOS 使用 Seatbelt (`sandbox-exec`)，Windows 使用 ACL 受限令牌并通过 C++ ABI probe (`verify/abi-probe.cpp`) 校验；目录选择器原生后端通过 worker 与 Win32 对话框交互；不变量配置使用 `@deepseek-ai/schemastery` 校验。