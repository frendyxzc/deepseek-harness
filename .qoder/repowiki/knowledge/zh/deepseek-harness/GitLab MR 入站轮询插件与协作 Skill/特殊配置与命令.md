通过 `scripts/setup-dsh/setup.sh` 一键安装（设置 `DSH_GITLAB_BOT_USERNAME` / `DSH_GITLAB_API_BASE` / `DSH_GITLAB_TOKEN`）；手动部署需将 `gitlab-mr-workflow` 复制到项目或用户 skills 目录，并在 `$DSH_HOME/profiles/web/cordis.patch.yml` 中以 `id: gitlab-mr` 挂载插件路径与配置。